import type { FC, PropsWithChildren } from 'react';

/**
 * Stub component to replace `@daveyplate/better-auth-ui/captcha`.
 * Renders nothing but preserves expected props so compilation succeeds.
 */
export const Captcha: FC<PropsWithChildren<{ action?: string; localization?: unknown }>> = () => {
  return null;
};